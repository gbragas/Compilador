programa
    declare a : number;
    declare b : number;
    declare c, d : text;
    inicio
        write("Hello World");
        read(c);
        a = 2 + 3.14 - 5 * 8.56 / a - c * a;
        write("The result is");
        read(b);
    fim
fimprog