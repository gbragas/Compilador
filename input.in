programa input
    declare a : number;
    declare b : number;
    declare c, d : text;
    inicio
        write("Hello World");
        read(a);
        b = 2 + 3.14 - 5 * 8.56 / a - b * a;
        write("The result is");

        if (a + 5 < b - 6) {
            b = a;
        } else {
            a = b;
        }
    fim
fimprog